# Docker build context exclusions — they speed up COPY . . and keep build outputs of the host
# machine out of the image.
**/bin/
**/obj/
**/.vs/
**/.git/
.git/
.github/
artifacts/
**/node_modules/
**/*.user
docs/
